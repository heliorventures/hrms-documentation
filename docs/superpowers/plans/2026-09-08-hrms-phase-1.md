# HRMS Phase 1 Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development for the independent layout task and its review; parent owns attendance data integration. Do not commit changes.

**Goal:** Correct monthly attendance figures and make the attendance, timesheet, leave and common dialogs usable with less scrolling.

**Architecture:** A self-bound attendance summary is computed from all authorized rows in the requested range and added to the existing board operation. Keep request ownership/race protection in the UI. Reorganize presentation using shared components without changing approval policies.

**Tech Stack:** React 18, TypeScript, GraphQL codegen, Rust async-graphql/SeaORM, Vitest.

**Spec:** ../specs/2026-09-08-hrms-enhancements.md

## Global constraints

- No commits, Dart/Flutter commands, live tenant writes, migrations or deployment.
- Preserve unrelated dirty work; scope backend edits to attendance and schema contracts in this phase.
- Use existing permission scopes and employee identity. Do not weaken API access to expose summaries.
- Keep desktop, tablet and mobile usable at 100% zoom, with focus and keyboard access.

### Task 1: Self attendance summary and board integration (parent)

Files: hrms-svc/crates/kabipay-attendance/src/services/{attendance_summary_service,attendance_duration,attendance_report_service,mod}.rs; resolvers/{query,types}.rs; hrms-ui/src/api/documents/clientOperations.graphql; src/api/schema-extensions/hrms-timesheet-attendance.graphql; generated GraphQL artifacts; src/modules/attendance/{AttendancePage,types}.tsx/ts and focused tests.

Interface: myAttendanceSummary(fromDate: NaiveDate!, toDate: NaiveDate!): AttendancePeriodSummary!, with completedMinutes: Int!, workedDays: Int!, averageMinutes: Float, incompleteSegments: Int!. Self-only identity comes from JWT, never caller employee ID. The board operation requests the summary with identical period variables.

- [x] Add regression tests for >50 completed segments, partial/open-only days, repeated workdates, month boundary overnight, canonical instants and legacy rows, invalid/reversed instants, and empty average.
- [x] Run focused Rust tests and observe failure before implementing summary.
- [x] Share existing canonical timestamp resolution with reporting; query complete period, validate range and exact access, accumulate duration and unique workdates.
- [x] Add schema extension and source operation; generate artifacts through codegen, using source-exported schema if the live gateway is unavailable.
- [x] Extend board mocks and test monthly totals remaining constant across cursor pages; verify month/account changes cannot show another snapshot.
- [x] Replace page statistics with server summary. Reorganize attendance layout with records before reference details.
- [x] Run Rust attendance tests, focused Vitest attendance tests and TypeScript build.

### Task 2: Compact timesheet, leave and dialogs (delegated)

Files owned: hrms-ui/src/modules/timesheet/**; src/modules/leave/**; src/components/common/Modal.tsx; src/modules/admin/components/{CreateEmployeeModal,AddEditEmployeeModal}.tsx; src/modules/attendance/components/ManualAttendanceModal.tsx; src/modules/expenses/components/SubmitExpenseModal.tsx; src/modules/notifications/CreateAnnouncementModal*.tsx; src/modules/workplace/assets/*Modal.tsx. Do not modify AttendancePage, attendance types, GraphQL files or backend.

Interfaces: retain existing component props/API mutations. Use Modal footer with form association for form submission where actions are outside the form. Retain busy/dismissal, permission, validation and stale-request protections.

- [x] Read existing components and tests; run their focused baseline tests.
- [x] Replace oversized TimesheetControlsCard layout with compact title/period/actions and contextual policy help. Keep accessible date/view labels and existing week submit protections.
- [x] Add a narrow-screen agenda layout alongside the existing weekly/month calendar, using the same selected rows and day-specific add/edit controls.
- [x] Put LeaveRequestsTableSection immediately after compact LeaveBalancesCard. Move holidays/types into named expandable sections.
- [x] Audit selected dialogs and standardize grouped responsive fields, purposeful widths and persistent actions; do not globally shrink controls/text.
- [x] Add only meaningful interaction tests needed for relocated actions/alternative layouts. Update existing assertions where approved visible copy changes.
- [x] Run focused Vitest tests and TypeScript check. Report modified files, verification, and residual concerns in docs/superpowers/reviews/2026-09-08-phase1-layout-report.md.

### Task 3: Review and verification

- [x] Independently review layout diff for spec compliance, responsive behavior and action accessibility. Resolve material findings.
- [x] Run build and changed-file lint plus git diff --check in affected repositories.
- [x] Connect browser using installed skill; inspect desktop/tablet/mobile if available. Record any connection/authentication limitation honestly.
- [x] Record source/test/browser evidence and rollout needs; leave all changes uncommitted.

## Execution record

- Initial UI branch: codex/ui-ux-modernization, clean. Service branch: main, clean; create a feature branch before edits. Existing database README/seed files are unrelated and untouched.
- No local gateway/attendance listener was found during preflight. UI dependencies are already installed.
- Phase 1 source implementation and scoped verification completed. Browser inspection is unavailable and lint is not clean; see ../reviews/2026-09-08-phase1-verification.md for exact evidence and limits. Checkboxes record work performed, not a claim that every gate passed.

